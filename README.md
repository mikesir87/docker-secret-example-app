# Docker Swarm Secret Example App

This project is only a simple application that is used to show secret management. Blog post can be found here.

```
docker run -p 3000:3000 mikesir87/docker-secret-example-app
```
